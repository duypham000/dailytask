extends layout/layout

block vars
	-title = 'Home'
	-bodyClass = 'home-page'

block container
	.searchBar
		input.active(type="text", placeholder="Search...")
		button.btn(type="button")
			i.fas.fa-search